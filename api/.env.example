# Configuração do Banco de Dados
# Para desenvolvimento local, use SQLite
DATABASE_URL="file:./dev.db"

# Para PostgreSQL (produção), use o formato:
# DATABASE_URL="postgresql://username:password@localhost:5432/nearby_db"

# Para MySQL (produção), use o formato:
# DATABASE_URL="mysql://username:password@localhost:3306/nearby_db"

# Configuração do Servidor
PORT=3333

# Configuração de Ambiente
NODE_ENV=development

# Configurações de CORS (opcional)
# ALLOWED_ORIGINS="http://localhost:3000,https://yourdomain.com"

# Configurações de Cache (opcional)
# CACHE_TTL=300000  # 5 minutos em milissegundos

# Configurações de Segurança (para produção)
# JWT_SECRET=your_super_secret_jwt_key_here
# API_KEY=your_api_key_here

# Observações:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Ajuste as variáveis conforme necessário
# 3. Nunca commite o arquivo .env no git
# 4. Para produção, use PostgreSQL ou MySQL ao invés de SQLite
