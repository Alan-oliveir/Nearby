# Configuração da API
# IP da máquina onde a API está rodando (desenvolvimento)
API_IP=192.168.1.5

# URL base da API (será construída automaticamente)
# Em desenvolvimento: http://${API_IP}:3333
# Em produção: configure a URL do servidor

# URL da API em produção (quando fazer deploy)
# API_PRODUCTION_URL=https://your-api-domain.com

# Configurações do Expo
# EXPO_PUBLIC_API_URL=http://192.168.1.5:3333

# Configurações de Mapas (se necessário)
# EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Configurações de Analytics (opcional)
# EXPO_PUBLIC_ANALYTICS_ID=your_analytics_id_here

# Observações:
# 1. Este arquivo é opcional para o projeto atual
# 2. O app detecta automaticamente o ambiente (dev/prod)
# 3. Para descobrir seu IP local, execute: cd ../api && npm run get-ip
# 4. Ajuste o IP em src/services/api.ts se necessário
